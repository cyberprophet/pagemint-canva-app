# engine-strict disabled: the project requires node ^20||^22||^24 per package.json
# but works correctly on node v25 (the active version in this environment).
# Re-enable when the CI/production environment is pinned to an LTS release.
engine-strict=false
